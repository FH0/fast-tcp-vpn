---
description: 直接回答用户的问题，不查看或修改项目代码。
---

当你被以 /ask 命令调用时，请遵循以下规则：

1. **直接回答**：直接回答用户的问题，无需进行冗长的项目分析。
2. **禁止查阅代码**：不要使用 `view_file`、`list_dir`、`grep_search` 等任何查看项目代码的工具。
3. **禁止修改代码**：不要使用 `write_to_file`、`replace_file_content` 等任何修改项目代码的工具。
4. **基于现有知识**：仅根据你自身的知识储备或用户在当前对话中提供的信息进行回答。
